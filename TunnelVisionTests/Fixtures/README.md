# TunnelVisionTests/Fixtures

Small, hand-crafted `.pcap` fixtures, one concern per file (e.g. `ipv4-tcp-syn.pcap`,
`ipv6-udp.pcap`, `tcp-out-of-order.pcap`). Keep them tiny and synthetic — **never commit real
personal traffic**; scrub or generate.
