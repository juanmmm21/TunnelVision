# Xcode
build/
DerivedData/
# Proyecto generado por XcodeGen desde project.yml (no se commitea).
*.xcodeproj/
# El Info.plist de la extensión también lo genera XcodeGen (sección `info:` de project.yml).
PacketTunnel/Info.plist
*.xcworkspace/
*.xcuserstate
*.xcuserdatad/
*.xcscmblueprint
xcuserdata/
*.moved-aside

# Swift Package Manager
.build/
.swiftpm/

# CocoaPods (si se usa en algún momento)
Pods/

# Configuración de editor / SO
.vscode/
.idea/
.DS_Store

# Variables de entorno y secretos
.env
.env.*
!.env.example

# Archivos de contexto para el asistente de IA (nunca se suben al repo)
AGENTS.md
CLAUDE.md
START_HERE.md
.claude/
