# TunnelVision/ViewModels

`@MainActor` view models feeding the screens: dashboard (live throughput), timeline
(paginated history), flow inspector, captures, settings. Consume the live-feed reader and the
`FlowStore`.

**Ref:** [`../../docs/ux/screens.md`](../../docs/ux/screens.md)
